package classe;

public class Produto {
	
	String nome;
	double preco;
	double desconto;
	
	//método para retornar o preço com desconto
	
	double precoComDesconto(double preco, double desconto) {
		double resultado = preco / (desconto * 0.1);
		return resultado;
	}
 
}
